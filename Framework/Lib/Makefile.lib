# Makefile used to build Webots files
LIBNAME?=badlibname
include ../../../Makefile.inc
LUA ?= lua

all: $(LIBNAME)

%.o: %.cpp
	$(CXX) $(CXXFLAGS) $(INCLUDES) -o $@ -c $<
%.o: %.cc
	$(CXX) $(CXXFLAGS) $(INCLUDES) -o $@ -c $<
%.o: %.c
	$(CC) $(CFLAGS) $(INCLUDES) -o $@ -c $<

$(LIBNAME): lua_$(LIBNAME).o
	$(LD) $(LDFLAGS) -o $@.$(SHLIBEXT) $^ $(LIBOPTS)
	rm $^

test: $(LIBNAME)
	@test ! -f $^.so && mv $^.$(SHLIBEXT) $^.so ; echo "Testing..."
	$(LUA) test_$^.lua
	@test ! -f $^.$(SHLIBEXT) && mv $^.so $^.$(SHLIBEXT) ; echo "Done the test!"

clean:
	rm -f *.o *.$(SHLIBEXT) *.$(MEXEXT)
